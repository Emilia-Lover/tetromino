초기 파이썬 설치 방법 
=======================
    cmd 관리자 권한으로 실행 
    winget pintall python.python.3.11
    이용 약관 동의 y 엔터
    python --version 3.11.x나오면 성공~
    py -m pip install pygame
<hr/>

저장 하는법
===========================
    git add .
    git commit -m "저장할것 이름"
    git push origin master
    git status # 상태 확인

<hr/>

집에서 할때
========================
    git cloen "주소" (vscord Terminal 창에서)
    add commit push

<hr/>

다시 기존에 있던곳에서 최신으로 불러올때
===========================
    git pull origin master (vscord Terminal 창에서)